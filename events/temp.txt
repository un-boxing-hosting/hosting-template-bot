const db = require('quick.db');
module.exports = {
    name: 'ready',
    once: false,
    async execute( dbb, bot,args) { //args are anything passed from the event
        var Discord = bot.Discord;
        
    },
};